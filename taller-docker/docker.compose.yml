version: '3.8'

services:
  web:
    image: kevinmartinez13/taller-docker:latest
    ports:
      - "5050:5050"

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - web